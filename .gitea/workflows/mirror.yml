name: Mirror to GitHub
on:
  push:
    branches:
      - main

jobs:
  mirror:
    runs-on: local
    steps:
      - name: Mirror to GitHub
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cd $(mktemp -d) 
          echo $GITHUB_TOKEN
          git clone --mirror https://gitea.shatto.cloud/Shatti/ha-custom-components.git .
          git remote add github https://${GITHUB_TOKEN}@github.com/CShatto90/ha-custom-components.git
          git push --mirror github